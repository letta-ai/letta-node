/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../mock-server/MockServerPool";
import { LettaClient } from "../../src/Client";

describe("Projects", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new LettaClient({ token: "test", project: "test", environment: server.baseUrl });

        const rawResponseBody = { projects: [{ name: "name", slug: "slug", id: "id" }], hasNextPage: true };
        server.mockEndpoint().get("/v1/projects").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.projects.list();
        expect(response).toEqual({
            projects: [
                {
                    name: "name",
                    slug: "slug",
                    id: "id",
                },
            ],
            hasNextPage: true,
        });
    });
});
